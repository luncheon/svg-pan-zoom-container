{
  "name": "svg-pan-zoom-select",
  "version": "0.0.0",
  "description": "",
  "license": "UNLICENSED",
  "private": true,
  "main": "index.js",
  "scripts": {
    "clean": "rimraf docs/",
    "build": "npm-run-all -s clean -p build:*",
    "build:iife": "rollup -c",
    "build:demo": "pug src/index.pug -o docs/",
    "watch": "npm-run-all -s clean -p watch:*",
    "watch:iife": "npm run build:iife -- -w",
    "watch:demo": "npm run build:demo -- -w"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "pug-cli": "^1.0.0-alpha6",
    "rimraf": "^2.6.3",
    "rollup": "^1.5.0",
    "rollup-plugin-typescript3": "^1.1.2",
    "typescript": "^3.3.3333"
  }
}
